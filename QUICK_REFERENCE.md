# 公告編輯系統 - 快速參考卡

## 🎯 三步快速開始

### 1️⃣ 訪問管理頁面
```
http://localhost:5176/#/admin
```
*注意：埠號會根據可用埠自動調整*

### 2️⃣ 登入
```
密碼: admin123
```

### 3️⃣ 新增公告
| 欄位 | 範例 | 必填 |
|------|------|------|
| **標題** | 春節服務公告 | ✅ |
| **日期** | 2026-02-05 | ✅ |
| **類型** | 重要/新消息/一般資訊 | ✅ |
| **標籤** | 服務, 時程 | ❌ |
| **內容** | 詳細說明... | ✅ |

---

## 🔑 密鑰資訊

### 文件位置
```
新增文件:
✅ src/composables/useAnnouncementManager.ts
✅ src/components/AdminPanel.vue
✅ src/views/AdminView.vue

修改文件:
📝 src/router/index.ts (新增 /admin 路由)
📝 src/components/AnnouncementBanner.vue (用動態 composable)
📝 src/components/AnnouncementsSection.vue (用動態 composable)
📝 .env (新增密碼配置)
```

### 路由
```
/admin                    → 公告管理後台
```

### 環境變數
```
VITE_ADMIN_PASSWORD       → 管理員密碼 (預設: admin123)
```

---

## 📊 公告類型顏色

| 類型 | 顏色代碼 | HTML 標籤 |
|------|---------|---------|
| important | 紅色 (Red 700) | `<Badge>重要</Badge>` |
| new | 綠色 (Emerald 700) | `<Badge>新</Badge>` |
| info | 藍色 (Blue 700) | `<Badge>資訊</Badge>` |

---

## ⚙️ 修改密碼

### 開發環境
編輯 `.env`:
```dotenv
VITE_ADMIN_PASSWORD=新密碼
```
重啟 `bun run dev`

### 生產環境
編輯 `.env.production`:
```dotenv
VITE_ADMIN_PASSWORD=新密碼
```
執行 `bun run build`

---

## 💾 備份與復原

### 備份 (導出 JSON)
```
1. 管理後台 → 點擊「導出」
2. 自動下載 announcements_2026-01-27.json
3. 保存到安全位置
```

### 復原 (導入 JSON)
```
1. 管理後台 → 點擊「導入」
2. 選擇備份的 JSON 檔案
3. 確認導入
```

### 重置為預設
```
管理後台 → 點擊「重置」
⚠️ 此操作無法撤銷！
```

---

## 📱 API Reference

### useAnnouncementManager Composable

```typescript
const {
  announcements,           // Ref<Announcement[]> - 公告列表
  addAnnouncement,         // 新增公告
  updateAnnouncement,      // 編輯公告
  deleteAnnouncement,      // 刪除公告
  resetToDefault,          // 重置為預設
  exportAsJson,            // 導出 JSON 字符串
  importFromJson,          // 導入 JSON 字符串
  stats                    // 統計資訊 (total, important, new, info)
} = useAnnouncementManager()
```

### Announcement 類型
```typescript
interface Announcement {
  id: number
  title: string
  date: string                      // "YYYY-MM-DD"
  type: 'important' | 'new' | 'info'
  tags?: string[]                   // 可選
  content: string
}
```

---

## 🐛 故障排除

### ❓ 公告沒有顯示？
- [ ] 確認是否點擊了「新增」
- [ ] 重新整理頁面 (Ctrl+F5)
- [ ] 檢查瀏覽器主控台 (F12 → Console)

### ❓ 登入失敗？
- [ ] 檢查密碼是否正確
- [ ] 確認 `.env` 中的密碼設定
- [ ] 清除瀏覽器快取

### ❓ 資料丟失？
- [ ] 從備份 JSON 導入復原
- [ ] 聯絡開發者重置

### ❓ 埠號不是 5173？
- [ ] Vite 會自動尋找可用埠
- [ ] 查看終端輸出找到實際埠號 (如 5176)

---

## 📞 聯絡方式

**開發者**: [您的名稱]  
**問題回報**: 查看 ANNOUNCEMENT_MANAGEMENT_GUIDE.md 的完整指南

---

## ✅ 檢查清單

部署前確認：
- [ ] 密碼已設定 (`.env`)
- [ ] 管理頁面能正常訪問 (`/#/admin`)
- [ ] 新增/編輯/刪除功能正常
- [ ] 備份文件已保存
- [ ] 首頁和公告頁面正常顯示公告

---

**最後更新**: 2026-01-27
